import logger from '@financial-times/n-logger';

export { autoLogOperation, autoLogController } from './operation';
export { autoLogAction, autoLogService } from './action';
export { loggerEvent } from './event';

export default logger;
